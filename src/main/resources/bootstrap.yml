spring:
  config:
    import: configserver:http://localhost:8111
#  cloud:
 #   config:
  #    discovery:
   #     enabled: true
    #    service-id: restaurant-config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka