spring:
  cloud:
    config:
      #uri: http://localhost:8888
      discovery:
        enabled: true
        service-id: restaurant-config-server
  application:
    name: restaurant-order
  profiles:
    active: prod


